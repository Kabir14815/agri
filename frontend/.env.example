# Copy to frontend/.env.local for local dev (optional — Vite proxy uses /api by default)
# For Netlify: set VITE_API_URL in Site settings → Environment variables
VITE_API_URL=https://agri-n5jz.onrender.com/api
